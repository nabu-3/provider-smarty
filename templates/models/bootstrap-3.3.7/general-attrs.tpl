{strip}
    {if isset($accesskey) && is_string($accesskey) && strlen($accesskey)>0} accesskey="{$accesskey}"{/if}
    {if isset($class) && is_string($class) && strlen($class)>0} class="{$class}"{/if}
    {if isset($contenteditable) && is_string($contenteditable) && strlen($contenteditable)>0} contenteditable="{$contenteditable}"{/if}
    {if isset($dir) && is_string($dir) && strlen($dir)>0} dir="{$dir}"{/if}
    {if isset($draggable) && is_string($draggable) && strlen($draggable)>0} draggable="{$draggable}"{/if}
    {if isset($dropzone) && is_string($dropzone) && strlen($dropzone)>0} dropzone="{$dropzone}"{/if}
    {if isset($hidden) && is_string($hidden) && strlen($hidden)>0} hidden="{$hidden}"{/if}
    {if isset($id) && is_string($id) && strlen($id)>0} id="{$id}"{/if}
    {if isset($lang) && is_string($lang) && strlen($lang)>0} lang="{$lang}"{/if}
    {if isset($spellcheck) && is_string($spellcheck) && strlen($spellcheck)>0} spellcheck="{$spellcheck}"{/if}
    {if isset($style) && is_string($style) && strlen($style)>0} style="{$style}"{/if}
    {if isset($tabindex) && is_string($tabindex) && strlen($tabindex)>0} tabindex="{$tabindex}"{/if}
    {if isset($title) && is_string($title) && strlen($title)>0} title="{$title}"{/if}
    {if isset($translate) && is_string($translate) && strlen($translate)>0} translate="{$translate}"{/if}
{/strip}
